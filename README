== Unexpected behavior in a join model observer ==

This project describes an unexpected behavior regarding join model observers.

For more details:
- See spec/observers/permission_observer_spec.rb

Since the tests cover 'after_commit' callbacks, the spec_helper was changed appropriately.